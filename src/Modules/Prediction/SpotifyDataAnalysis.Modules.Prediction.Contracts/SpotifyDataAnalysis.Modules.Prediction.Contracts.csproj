<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!--
    Contracts: fronteira PÚBLICA do módulo Prediction para OUTROS módulos/consumidores (DTOs achatados e
    integration events). Depende só do SharedKernel. Nada de ML.NET/EF/Domain/Application. Os DTOs públicos
    chegam quando a inferência (E3.5) e o dashboard (E5) consumirem o Prediction; por ora carrega apenas o
    marcador de assembly.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\Shared\SpotifyDataAnalysis.SharedKernel\SpotifyDataAnalysis.SharedKernel.csproj" />
  </ItemGroup>

</Project>
