## .NET / build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user
*.suo

## Rider / IDE
.idea/
.vs/
*.DotSettings.user

## Test / coverage
[Tt]est[Rr]esult*/
coverage*.json
coverage*.xml
*.coverage

## App secrets / local config (never commit)
appsettings.*.local.json
secrets.json
# Connection strings / credenciais vão para User Secrets ou variáveis de ambiente — nunca no repo.

## Docs / planejamento (não publicados no portfólio)
docs/

## Claude Code (tooling local — agentes e settings da máquina)
.claude/

## Dataset / modelos de ML (dados locais e artefatos — nunca versionar)
# O CSV do Kaggle (~20 MB) e os modelos treinados (.zip/.mlnet) ficam no disco, fora do git.
# O padrao de artefatos e ANCORADO na raiz: sem a barra inicial, "models/" casa com QUALQUER pasta
# Models/ da solucao e engole codigo-fonte — foi o que aconteceu com o agregado do E3.4.
/dataset.csv
*.mlnet
/models/

## Frontend (quando existir)
node_modules/
frontend/dist/

## OS
.DS_Store
Thumbs.db
